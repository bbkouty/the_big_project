<%# app/views/comments/edit.html.erb #%>

<div class="container mt-4">
  <div class="card shadow-sm">
    <div class="card-body">
      <h3 class="card-title text-primary">Modifier le commentaire</h3>

      <p>
        👤 Auteur : 
        <strong><%= @comment.user.first_name %> <%= @comment.user.last_name %></strong>
      </p>

      <!-- Formulaire de mise à jour -->
      <%= form_with(model: [@comment.commentable, @comment], local: true) do |f| %>
        <div class="mb-3">
          <%= f.label :content, "Votre commentaire", class: "form-label" %>
          <%= f.text_area :content, class: "form-control", rows: 4 %>
        </div>

        <div class="d-flex gap-2">
          <%= f.submit "Mettre à jour", class: "btn btn-success" %>
          <%= link_to "Annuler", gossip_path(@comment.commentable), class: "btn btn-secondary" %>
        </div>
      <% end %>

      <!-- Bouton suppression séparé -->
      <div class="mt-2">
        <%= button_to "Supprimer", gossip_comment_path(@comment.commentable, @comment), method: :delete, data: { confirm: "Êtes-vous sûr de vouloir supprimer ce commentaire ?" }, class: "btn btn-danger" %>
      </div>
    </div>
  </div>
</div>
